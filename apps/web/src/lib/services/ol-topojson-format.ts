import { topoJsonDragFormat as CoreTopoJsonDragFormat } from '@geovara/core';

/**
 * Minimal OL-style format for DragAndDrop: TopoJSON text → features in map projection.
 * Now acts as a proxy to @geovara/core.
 */
export const topoJsonDragFormat = CoreTopoJsonDragFormat;
